{% doc %}
  @prompt
    You are to generate the complete code for a new, self-contained Shopify theme section in a single Liquid file. The section shall be named 'hero-video-nexus.liquid'.
    
    The section's purpose is to display a full-width, auto-playing video background with an editable text and button overlay.
    
    The architecture must adhere to these absolute specifications:
    
    **1. HTML Structure:**
       - A main wrapper `div` for the section.
       - An inner `div` to act as the container.
       - A `<video>` element for the background. This element must be configured to `autoplay`, `loop`, `muted`, and `playsinline`.
       - A `div` to serve as a dark overlay, positioned on top of the video but behind the text.
       - A final `div` for the content payload (heading, subheading, button), centered vertically and horizontally.
    
    **2. CSS Styling:**
       - The section must be full-width and its height should be a significant portion of the viewport (e.g., 75vh).
       - The video element must be styled with `position: absolute`, `top: 0`, `left: 0`, `width: 100%`, `height: 100%`, `object-fit: cover`, and a `z-index` that places it at the very back.
       - The content payload `div` must be positioned in the center of the section using Flexbox or Grid, with a `z-index` that ensures it is always on top of the video and the dark overlay.
       - The text must be white and easily readable against a potentially complex video.
    
    **3. Shopify Schema (`{% schema %}`):**
       The section must be fully configurable through the Shopify Theme Editor. The schema must be structured as follows:
    
       - **Name:** "Hero Video Nexus"
       - **Presets:** Include one preset named "Hero Video Nexus".
    
       - **Settings Group: "Media"**
         - A `video_url` setting named "Video URL". Include info text: "Shopify-hosted MP4 URL."
         - An `image_picker` setting named "Fallback Image". Include info text: "Shows on mobile or if the video fails to load."
    
       - **Settings Group: "Content"**
         - A `text` setting for the "Heading" with a default value.
     
{% enddoc %}
{% assign ai_gen_id = block.id | replace: '_', '' | downcase %}

{% style %}
  .hero-video-nexus-{{ ai_gen_id }} {
    position: relative;
    width: 100%;
    height: {{ block.settings.section_height }}vh;
    overflow: hidden;
  }

  .hero-video-nexus__video-{{ ai_gen_id }} {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    z-index: 1;
  }

  .hero-video-nexus__overlay-{{ ai_gen_id }} {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-color: {{ block.settings.overlay_color }};
    opacity: {{ block.settings.overlay_opacity | divided_by: 100.0 }};
    z-index: 2;
  }

  .hero-video-nexus__content-{{ ai_gen_id }} {
    position: relative;
    z-index: 3;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    height: 100%;
    text-align: center;
    padding: 20px;
  }

  .hero-video-nexus__heading-{{ ai_gen_id }} {
    color: {{ block.settings.heading_color }};
    font-size: {{ block.settings.heading_size }}px;
    margin: 0 0 {{ block.settings.spacing }}px 0;
    max-width: 800px;
  }

  .hero-video-nexus__subheading-{{ ai_gen_id }} {
    color: {{ block.settings.subheading_color }};
    font-size: {{ block.settings.subheading_size }}px;
    margin: 0 0 {{ block.settings.spacing }}px 0;
    max-width: 600px;
  }

  .hero-video-nexus__button-{{ ai_gen_id }} {
    display: inline-block;
    padding: {{ block.settings.button_padding_vertical }}px {{ block.settings.button_padding_horizontal }}px;
    background-color: {{ block.settings.button_background }};
    color: {{ block.settings.button_text_color }};
    text-decoration: none;
    border-radius: {{ block.settings.button_border_radius }}px;
    font-size: {{ block.settings.button_font_size }}px;
    transition: background-color 0.3s ease;
  }

  .hero-video-nexus__button-{{ ai_gen_id }}:hover {
    background-color: {{ block.settings.button_hover_background }};
  }

  .hero-video-nexus__fallback-{{ ai_gen_id }} {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    z-index: 1;
  }

  @media screen and (max-width: 749px) {
    .hero-video-nexus__video-{{ ai_gen_id }} {
      display: none;
    }

    .hero-video-nexus__heading-{{ ai_gen_id }} {
      font-size: {{ block.settings.heading_size | times: 0.6 }}px;
    }

    .hero-video-nexus__subheading-{{ ai_gen_id }} {
      font-size: {{ block.settings.subheading_size | times: 0.8 }}px;
    }
  }

  @media screen and (min-width: 750px) {
    .hero-video-nexus__fallback-{{ ai_gen_id }} {
      display: none;
    }
  }
{% endstyle %}

<div class="hero-video-nexus-{{ ai_gen_id }}" {{ block.shopify_attributes }}>
  {% if block.settings.video_url != blank %}
    <video
      class="hero-video-nexus__video-{{ ai_gen_id }}"
      autoplay
      loop
      muted
      playsinline
    >
      <source src="{{ block.settings.video_url }}" type="video/mp4">
    </video>
  {% endif %}

  {% if block.settings.fallback_image %}
    <img
      src="{{ block.settings.fallback_image | image_url: width: 2000 }}"
      alt="{{ block.settings.fallback_image.alt | escape }}"
      class="hero-video-nexus__fallback-{{ ai_gen_id }}"
      loading="lazy"
      width="{{ block.settings.fallback_image.width }}"
      height="{{ block.settings.fallback_image.height }}"
    >
  {% endif %}

  <div class="hero-video-nexus__overlay-{{ ai_gen_id }}"></div>

  <div class="hero-video-nexus__content-{{ ai_gen_id }}">
    {% if block.settings.heading != blank %}
      <h1 class="hero-video-nexus__heading-{{ ai_gen_id }}">{{ block.settings.heading }}</h1>
    {% endif %}

    {% if block.settings.subheading != blank %}
      <p class="hero-video-nexus__subheading-{{ ai_gen_id }}">{{ block.settings.subheading }}</p>
    {% endif %}

    {% if block.settings.button_text != blank and block.settings.button_link != blank %}
      <a href="{{ block.settings.button_link }}" class="hero-video-nexus__button-{{ ai_gen_id }}">
        {{ block.settings.button_text }}
      </a>
    {% endif %}
  </div>
</div>

{% schema %}
{
  "name": "Hero video nexus",
  "settings": [
    {
      "type": "header",
      "content": "Media"
    },
    {
      "type": "url",
      "id": "video_url",
      "label": "Video URL",
      "info": "Shopify-hosted MP4 URL."
    },
    {
      "type": "image_picker",
      "id": "fallback_image",
      "label": "Fallback image",
      "info": "Shows on mobile or if the video fails to load."
    },
    {
      "type": "header",
      "content": "Content"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Welcome to our store"
    },
    {
      "type": "text",
      "id": "subheading",
      "label": "Subheading",
      "default": "Discover amazing products"
    },
    {
      "type": "text",
      "id": "button_text",
      "label": "Button text",
      "default": "Shop now"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "Button link"
    },
    {
      "type": "header",
      "content": "Layout"
    },
    {
      "type": "range",
      "id": "section_height",
      "min": 40,
      "max": 100,
      "step": 5,
      "unit": "vh",
      "label": "Section height",
      "default": 75
    },
    {
      "type": "range",
      "id": "spacing",
      "min": 10,
      "max": 50,
      "step": 5,
      "unit": "px",
      "label": "Content spacing",
      "default": 20
    },
    {
      "type": "header",
      "content": "Overlay"
    },
    {
      "type": "color",
      "id": "overlay_color",
      "label": "Color",
      "default": "#000000"
    },
    {
      "type": "range",
      "id": "overlay_opacity",
      "min": 0,
      "max": 100,
      "step": 5,
      "unit": "%",
      "label": "Opacity",
      "default": 40
    },
    {
      "type": "header",
      "content": "Heading"
    },
    {
      "type": "color",
      "id": "heading_color",
      "label": "Color",
      "default": "#ffffff"
    },
    {
      "type": "range",
      "id": "heading_size",
      "min": 20,
      "max": 100,
      "step": 2,
      "unit": "px",
      "label": "Size",
      "default": 48
    },
    {
      "type": "header",
      "content": "Subheading"
    },
    {
      "type": "color",
      "id": "subheading_color",
      "label": "Color",
      "default": "#ffffff"
    },
    {
      "type": "range",
      "id": "subheading_size",
      "min": 12,
      "max": 40,
      "step": 2,
      "unit": "px",
      "label": "Size",
      "default": 18
    },
    {
      "type": "header",
      "content": "Button"
    },
    {
      "type": "color",
      "id": "button_background",
      "label": "Background",
      "default": "#2563eb"
    },
    {
      "type": "color",
      "id": "button_hover_background",
      "label": "Hover background",
      "default": "#3b82f6"
    },
    {
      "type": "color",
      "id": "button_text_color",
      "label": "Text color",
      "default": "#ffffff"
    },
    {
      "type": "range",
      "id": "button_font_size",
      "min": 12,
      "max": 24,
      "step": 1,
      "unit": "px",
      "label": "Font size",
      "default": 16
    },
    {
      "type": "range",
      "id": "button_padding_vertical",
      "min": 8,
      "max": 30,
      "step": 2,
      "unit": "px",
      "label": "Vertical padding",
      "default": 14
    },
    {
      "type": "range",
      "id": "button_padding_horizontal",
      "min": 16,
      "max": 60,
      "step": 4,
      "unit": "px",
      "label": "Horizontal padding",
      "default": 32
    },
    {
      "type": "range",
      "id": "button_border_radius",
      "min": 0,
      "max": 50,
      "step": 2,
      "unit": "px",
      "label": "Border radius",
      "default": 0
    }
  ],
  "presets": [
    {
      "name": "Hero video nexus"
    }
  ]
}
{% endschema %}