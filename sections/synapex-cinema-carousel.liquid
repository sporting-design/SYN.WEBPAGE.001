{% comment %}
  ╔════════════════════════════════════════════════════════════════╗
  ║  SYNAPEX CINEMA CAROUSEL (V2 - MASTER)                         ║
  ║  Nike-Grade Architecture: Full Bleed, Direct Overlay           ║
  ║  Forged by: Satori Systems                                     ║
  ╚════════════════════════════════════════════════════════════════╝
{% endcomment %}

{%- liquid
  assign section_id = 'synapex-cinema-' | append: section.id
  assign container_height = section.settings.container_height | default: '100svh'
  assign slide_count = section.blocks.size
-%}

<style>
  /* ═══════════════════════════════════════════════════════════════
     CSS VARIABLES & LAYOUT
  ═══════════════════════════════════════════════════════════════ */
  #{{ section_id }} {
    --cinema-height: {{ container_height }};
    --cinema-transition: {{ section.settings.transition_speed }}ms;
    --dot-size: 10px; /* Minimalist */
    --dot-gap: 12px;
    /* Global Text Shadow for contrast without boxes */
    --text-shadow: 0 2px 10px rgba(0,0,0,0.5); 
  }

  .synapex-cinema {
    position: relative;
    width: 100%;
    height: var(--cinema-height);
    overflow: hidden;
    background: #000;
  }

  /* ═══════════════════════════════════════════════════════════════
     SLIDE LOGIC
  ═══════════════════════════════════════════════════════════════ */
  .cinema-slide {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    opacity: 0;
    visibility: hidden;
    transition: opacity var(--cinema-transition) ease-in-out;
    z-index: 1;
  }

  .cinema-slide.active {
    opacity: 1;
    visibility: visible;
    z-index: 2;
  }

  /* ═══════════════════════════════════════════════════════════════
     MEDIA LAYER
  ═══════════════════════════════════════════════════════════════ */
  .cinema-media {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  /* Scoped Visibility */
  .cinema-video-desktop { display: block; }
  .cinema-video-mobile { display: none; }

  @media (max-width: 768px) {
    .cinema-video-desktop { display: none; }
    .cinema-video-mobile { display: block; }
  }

  /* Global Overlay (Optional Darkening) */
  .cinema-overlay {
    position: absolute;
    inset: 0;
    background: rgba(0,0,0, {{ section.settings.overlay_opacity | divided_by: 100.0 }});
    z-index: 2;
    pointer-events: none;
  }

  /* ═══════════════════════════════════════════════════════════════
     TEXT CONTENT (NO BOXES - PURE TYPOGRAPHY)
  ═══════════════════════════════════════════════════════════════ */
  .cinema-content {
    position: absolute;
    z-index: 3;
    width: 100%;
    height: 100%;
    padding: 60px;
    display: flex;
    pointer-events: none; /* Let clicks pass through */
  }

  .content-wrapper {
    pointer-events: auto;
    max-width: 700px;
    text-align: left; /* Default */
  }

  /* POSITIONING MATRIX */
  .pos-top-left      { align-items: flex-start; justify-content: flex-start; }
  .pos-top-center    { align-items: flex-start; justify-content: center; text-align: center; }
  .pos-top-right     { align-items: flex-start; justify-content: flex-end; text-align: right; }
  .pos-center-left   { align-items: center;     justify-content: flex-start; }
  .pos-center        { align-items: center;     justify-content: center; text-align: center; }
  .pos-center-right  { align-items: center;     justify-content: flex-end; text-align: right; }
  .pos-bottom-left   { align-items: flex-end;   justify-content: flex-start; }
  .pos-bottom-center { align-items: flex-end;   justify-content: center; text-align: center; }
  .pos-bottom-right  { align-items: flex-end;   justify-content: flex-end; text-align: right; }

  /* TYPOGRAPHY */
  .cinema-heading {
    font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif; /* Update to brand font */
    font-size: clamp(2.5rem, 5vw, 5rem); /* Responsive Scaling */
    font-weight: 900;
    text-transform: uppercase;
    line-height: 0.9;
    color: #fff;
    margin-bottom: 1rem;
    text-shadow: var(--text-shadow);
    letter-spacing: -0.02em;
  }

  .cinema-subheading {
    font-size: clamp(1rem, 1.5vw, 1.25rem);
    font-weight: 500;
    color: #fff;
    margin-bottom: 2rem;
    text-shadow: var(--text-shadow);
    max-width: 500px;
  }

  /* CENTER TEXT ALIGNMENT FIX */
  .pos-top-center .cinema-subheading,
  .pos-center .cinema-subheading,
  .pos-bottom-center .cinema-subheading {
    margin-left: auto;
    margin-right: auto;
  }

  .pos-top-right .cinema-subheading,
  .pos-center-right .cinema-subheading,
  .pos-bottom-right .cinema-subheading {
    margin-left: auto;
    margin-right: 0;
  }

  /* BUTTON - TACTICAL */
  .cinema-button {
    display: inline-block;
    padding: 16px 36px;
    background: #fff;
    color: #000;
    font-weight: 800;
    text-transform: uppercase;
    text-decoration: none;
    border-radius: 4px; /* Sharp tactical corners */
    transition: transform 0.2s ease, background 0.2s;
    box-shadow: 0 4px 15px rgba(0,0,0,0.3);
  }
  
  .cinema-button:hover {
    transform: translateY(-2px);
    background: #f0f0f0;
  }

  @media (max-width: 768px) {
    .cinema-content { padding: 30px; }
    .cinema-button { padding: 14px 28px; font-size: 0.9rem; }
  }

  /* ═══════════════════════════════════════════════════════════════
     NAV DOTS (BOTTOM RIGHT - NIKE STYLE)
  ═══════════════════════════════════════════════════════════════ */
  .cinema-nav {
    position: absolute;
    bottom: 40px;
    right: 40px;
    z-index: 10;
    display: flex;
    gap: var(--dot-gap);
  }

  .cinema-dot {
    width: var(--dot-size);
    height: var(--dot-size);
    border-radius: 50%;
    border: 1.5px solid #fff;
    background: transparent;
    cursor: pointer;
    padding: 0;
    transition: all 0.3s ease;
  }

  .cinema-dot.active {
    background: #fff;
    transform: scale(1.2);
  }

  @media (max-width: 768px) {
    .cinema-nav { bottom: 20px; right: 20px; }
  }

  /* HIDE IF EMPTY */
  {% if slide_count == 0 %} #{{ section_id }} { display: none; } {% endif %}
</style>

<section id="{{ section_id }}" class="synapex-cinema">
  
  {% comment %} RENDER SLIDES {% endcomment %}
  {% for block in section.blocks %}
    {%- liquid
      assign is_first = forloop.first
      assign pos_class = 'pos-' | append: block.settings.text_position
    -%}

    <div 
      class="cinema-slide{% if is_first %} active{% endif %}" 
      data-index="{{ forloop.index0 }}"
      {{ block.shopify_attributes }}
    >
      
      {% comment %} MEDIA LAYER {% endcomment %}
      <div class="cinema-media-wrapper">
        {% comment %} DESKTOP {% endcomment %}
        {% if block.settings.video_desktop != blank %}
          <video class="cinema-media cinema-video-desktop" autoplay muted loop playsinline {% unless is_first %}data-lazy{% endunless %}>
            <source src="{{ block.settings.video_desktop }}" type="video/mp4">
          </video>
        {% elsif block.settings.image_fallback != blank %}
          {{ block.settings.image_fallback | image_url: width: 2000 | image_tag: class: 'cinema-media cinema-video-desktop', loading: 'lazy' }}
        {% endif %}

        {% comment %} MOBILE {% endcomment %}
        {% if block.settings.video_mobile != blank %}
          <video class="cinema-media cinema-video-mobile" autoplay muted loop playsinline {% unless is_first %}data-lazy{% endunless %}>
            <source src="{{ block.settings.video_mobile }}" type="video/mp4">
          </video>
        {% elsif block.settings.video_desktop != blank %}
          <video class="cinema-media cinema-video-mobile" autoplay muted loop playsinline {% unless is_first %}data-lazy{% endunless %}>
            <source src="{{ block.settings.video_desktop }}" type="video/mp4">
          </video>
        {% elsif block.settings.image_fallback != blank %}
          {{ block.settings.image_fallback | image_url: width: 1000 | image_tag: class: 'cinema-media cinema-video-mobile', loading: 'lazy' }}
        {% endif %}
      </div>

      <div class="cinema-overlay"></div>

      {% comment %} TEXT LAYER {% endcomment %}
      <div class="cinema-content {{ pos_class }}">
        <div class="content-wrapper">
          {% if block.settings.heading != blank %}
            <h2 class="cinema-heading">{{ block.settings.heading }}</h2>
          {% endif %}
          
          {% if block.settings.subheading != blank %}
            <p class="cinema-subheading">{{ block.settings.subheading }}</p>
          {% endif %}
          
          {% if block.settings.button_label != blank %}
            <a href="{{ block.settings.button_link }}" class="cinema-button">{{ block.settings.button_label }}</a>
          {% endif %}
        </div>
      </div>

    </div>
  {% endfor %}

  {% comment %} NAVIGATION {% endcomment %}
  {% if slide_count > 1 %}
    <div class="cinema-nav">
      {% for block in section.blocks %}
        <button 
          class="cinema-dot{% if forloop.first %} active{% endif %}" 
          data-target="{{ forloop.index0 }}"
          aria-label="Go to slide {{ forloop.index }}"
        ></button>
      {% endfor %}
    </div>
  {% endif %}

</section>

<script>
  (function() {
    const container = document.getElementById('{{ section_id }}');
    if (!container) return;

    const slides = container.querySelectorAll('.cinema-slide');
    const dots = container.querySelectorAll('.cinema-dot');
    let currentIndex = 0;
    let autoplayTimer;
    const autoplayEnabled = {{ section.settings.autoplay | default: false }};
    const autoplaySpeed = {{ section.settings.autoplay_speed | default: 5000 }};

    function activateSlide(index) {
      if (index === currentIndex) return;
      
      // Deactivate Current
      slides[currentIndex].classList.remove('active');
      if(dots[currentIndex]) dots[currentIndex].classList.remove('active');
      
      // Pause Video
      const prevVideo = slides[currentIndex].querySelectorAll('video');
      prevVideo.forEach(v => v.pause());

      // Activate New
      currentIndex = index;
      slides[currentIndex].classList.add('active');
      if(dots[currentIndex]) dots[currentIndex].classList.add('active');

      // Play Video & Handle Lazy Load
      const nextVideos = slides[currentIndex].querySelectorAll('video');
      nextVideos.forEach(v => {
        if (v.hasAttribute('data-lazy')) {
          v.removeAttribute('data-lazy');
          v.load();
        }
        v.play().catch(e => console.log('Autoplay blocked:', e));
      });
    }

    // Event Listeners
    dots.forEach(dot => {
      dot.addEventListener('click', () => {
        activateSlide(parseInt(dot.dataset.target));
        resetTimer();
      });
    });

    // Autoplay Logic
    function startTimer() {
      if (!autoplayEnabled) return;
      autoplayTimer = setInterval(() => {
        const next = (currentIndex + 1) % slides.length;
        activateSlide(next);
      }, autoplaySpeed);
    }

    function resetTimer() {
      clearInterval(autoplayTimer);
      startTimer();
    }

    // Init
    startTimer();

  })();
</script>

{% schema %}
{
  "name": "Cinema Carousel (Nike)",
  "settings": [
    {
      "type": "select",
      "id": "container_height",
      "label": "Height",
      "options": [
        { "value": "100svh", "label": "Full Screen (100svh)" },
        { "value": "85vh", "label": "High (85vh)" },
        { "value": "600px", "label": "Fixed (600px)" }
      ],
      "default": "100svh"
    },
    {
      "type": "range",
      "id": "overlay_opacity",
      "min": 0,
      "max": 90,
      "step": 5,
      "unit": "%",
      "label": "Overlay Darkness",
      "default": 20
    },
    {
      "type": "range",
      "id": "transition_speed",
      "min": 300,
      "max": 1000,
      "step": 100,
      "unit": "ms",
      "label": "Transition Speed",
      "default": 600
    },
    {
      "type": "checkbox",
      "id": "autoplay",
      "label": "Autoplay Slides",
      "default": true
    },
    {
      "type": "range",
      "id": "autoplay_speed",
      "min": 3000,
      "max": 10000,
      "step": 500,
      "unit": "ms",
      "label": "Autoplay Speed",
      "default": 5000
    }
  ],
  "blocks": [
    {
      "type": "slide",
      "name": "Slide",
      "settings": [
        {
          "type": "header",
          "content": "Media (MP4 Links)"
        },
        {
          "type": "url",
          "id": "video_desktop",
          "label": "Desktop Video URL",
          "info": "Paste link to MP4 file from Settings > Files"
        },
        {
          "type": "url",
          "id": "video_mobile",
          "label": "Mobile Video URL",
          "info": "Paste link to Portrait MP4 file"
        },
        {
          "type": "image_picker",
          "id": "image_fallback",
          "label": "Fallback Image"
        },
        {
          "type": "header",
          "content": "Text"
        },
        {
          "type": "text",
          "id": "heading",
          "label": "Heading",
          "default": "UNCOMPROMISED"
        },
        {
          "type": "textarea",
          "id": "subheading",
          "label": "Subheading",
          "default": "The standard for athlete performance."
        },
        {
          "type": "text",
          "id": "button_label",
          "label": "Button Label",
          "default": "SHOP NOW"
        },
        {
          "type": "url",
          "id": "button_link",
          "label": "Button Link"
        },
        {
          "type": "select",
          "id": "text_position",
          "label": "Position",
          "options": [
            { "value": "top-left", "label": "Top Left" },
            { "value": "top-center", "label": "Top Center" },
            { "value": "top-right", "label": "Top Right" },
            { "value": "center-left", "label": "Center Left" },
            { "value": "center", "label": "Center" },
            { "value": "center-right", "label": "Center Right" },
            { "value": "bottom-left", "label": "Bottom Left" },
            { "value": "bottom-center", "label": "Bottom Center" },
            { "value": "bottom-right", "label": "Bottom Right" }
          ],
          "default": "bottom-left"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Cinema Carousel (Nike)",
      "blocks": [
        { "type": "slide" }
      ]
    }
  ]
}
{% endschema %}